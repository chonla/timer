import { ReadableStatePipe } from './readable-state.pipe';

describe('ReadableStatePipe', () => {
  it('create an instance', () => {
    const pipe = new ReadableStatePipe();
    expect(pipe).toBeTruthy();
  });
});
