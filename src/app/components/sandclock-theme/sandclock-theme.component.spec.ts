import { SandclockThemeComponent } from './sandclock-theme.component';

describe('SandclockThemeComponent', () => {
  let component: SandclockThemeComponent;

  beforeEach(() => {
    component = new SandclockThemeComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
