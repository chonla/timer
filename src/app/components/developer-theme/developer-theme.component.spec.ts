import { DeveloperThemeComponent } from './developer-theme.component';

describe('DeveloperThemeComponent', () => {
  let component: DeveloperThemeComponent;

  beforeEach(() => {
    component = new DeveloperThemeComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
